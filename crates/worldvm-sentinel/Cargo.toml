[package]
name = "worldvm-sentinel"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
description = "Autonomous adaptive threat detector, behavioral profiler, and tarpit shield for WorldVM."

[dependencies]
worldvm-core = { path = "../worldvm-core" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
sha2 = "0.10"
hex = "0.4"
parking_lot = "0.12"
thiserror = "2.0"
