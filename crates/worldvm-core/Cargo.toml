[package]
name = "worldvm-core"
version = "1.0.0"
edition = "2021"
authors = ["WorldVM Contributors"]
description = "Core types, entity handles, error definitions, and metrics for WorldVM"
license = "Apache-2.0"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "2.0"
tracing = "0.1"
