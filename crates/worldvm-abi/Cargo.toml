[package]
name = "worldvm-abi"
version = "1.0.0"
edition = "2021"
authors = ["WorldVM Contributors"]
description = "WorldVM ABI v1: Host-guest interface specifications, event payloads, and capability types"
license = "Apache-2.0"

[dependencies]
worldvm-core = { path = "../worldvm-core" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "2.0"
