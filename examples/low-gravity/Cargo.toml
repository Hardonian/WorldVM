[package]
name = "low-gravity"
version = "1.3.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
worldvm-sdk = { path = "../../crates/worldvm-sdk" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
